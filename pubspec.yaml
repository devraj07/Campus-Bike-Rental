name: campus_bike_rental
description: Campus Bike Rental – IITGN

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  flutter_animate: ^4.3.0
  percent_indicator: ^4.2.3
  image_picker: ^1.0.7
  intl: ^0.19.0
  firebase_core: ^3.3.0
  cloud_firestore: ^5.2.1
  firebase_auth: ^5.1.4
  firebase_storage: ^12.1.0
  mailer: ^6.1.2
  mqtt_client: ^10.5.1
  razorpay_flutter: ^1.3.6
  shared_preferences: ^2.3.2
  mqtt_client: ^10.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
